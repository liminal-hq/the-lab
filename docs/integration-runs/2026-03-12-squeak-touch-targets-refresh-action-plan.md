# PR Integration Action Plan Artefact

## Purpose

Audit of the current open PR queue for `liminal-hq/the-lab`, focused on the refreshed squeak-and-modal stack now represented by `#101` and `#102`.

```text
  __
 (oo)  Rat says: keep the stack tidy and merge the narrower squeaks first.
 /\/\
```

## Run Metadata

- Run date: `2026-03-12`
- Repository: `liminal-hq/the-lab`
- Mode: `audit`
- Requested by: `scott`
- Generated by: `Codex GPT-5`
- Planned integration branch: `integration/squeak-touch-targets-refresh`
- Branch naming check: `pass`

## Main Snapshot

- Current branch: `main`
- Current `main` head: `43dfd44` (`Document March 12 integration run and shell safety guidance (#100)`)
- Working tree clean: `yes`
- Audit started from `main`: `yes`
- Branch protection: `none configured`
- Allowed merge methods: `merge`, `rebase`

## Queue Summary

Open queue shape:

- Active automated queue: `#101`, `#102`
- Superseded consolidation queue: `#99`
- Deferred manual queue: `#14`

Audit conclusion:

1. `#101` and `#102` are the current merge candidates: both are `CLEAN`, narrowly scoped, and align with the intended gameplay and modal UX updates.
2. `#99` overlaps both active PRs but carries older, broader consolidation scope that is no longer the preferred representation of this work.
3. `#102` needs a Conventional Commit rewrite if merged directly; using an integration branch avoids rewriting the source branch.
4. `#14` remains outside this run and should keep `manual-follow-up`.

## PR Inventory

1. `#101` `🧱 Scurry: [squeak mechanic]`
   - Branch: `scurry-squeak-mechanic-1844732970679861154` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `none`
   - Changed files: `3`
2. `#102` `🎨🐀 Palette-Rat: [UX improvement] Increase touch targets and add modal overlay dismissal`
   - Branch: `palette-rat-touch-targets-modals-1212973008546341614` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `none`
   - Changed files: `2`
3. `#99` `Add bird-scare squeak and improve modal touch UX (#97, #98)`
   - Branch: `integration/bird-squeak-modal-ux` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `documentation`, `consolidation`, `accessibility`, `ux`, `gameplay`
   - Changed files: `6`
4. `#14` `Add Rats Music Converter`
   - Branch: `rats-music-converter-9930008502543285091` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `documentation`, `enhancement`, `ci`, `manual-follow-up`
   - Changed files: `15`

## Scoring

Scoring notes:

1. This model gives explicit positive weight to UX improvements but not to net-new gameplay mechanics, so `#101` scores neutrally unless overlap risk is applied.
2. Overlap penalties are applied where a PR collides with higher-priority current-stack work.

1. `#102`: `30`
2. `#101`: `0`
3. `#99`: `-10`
4. `#14`: `-70`

## Conflict Matrix

| File | PRs touching file | Conflict risk |
| --- | --- | --- |
| `rats-the-video-game/index.html` | `#99, #101, #102` | `high` |
| `rats-the-video-game/src/game.js` | `#99, #101` | `medium` |
| `.Jules/scurry.md` | `#99, #101` | `medium` |
| `.Jules/palette-rat.md` | `#99, #102` | `medium` |
| `.Jules/ink.md` | `#99` | `low` |
| `.Jules/nibble.md` | `#99` | `low` |

Conflict resolution notes:

1. Merge `#101` before `#102` so gameplay and help-text changes settle before the modal UX patch lands on the same HTML file.
2. Treat `#99` as superseded rather than merging it alongside `#101` and `#102`; otherwise the batch would duplicate the same product changes and reintroduce broader scope.
3. Keep `#14` out of this batch entirely because it is already labelled `manual-follow-up` and remains `DIRTY` against current `main`.

## Allowed-File Guardrail Check

1. `#101`
   - Expected: `rats-the-video-game/src/game.js`, `rats-the-video-game/index.html`, `.Jules/scurry.md`
   - Unexpected: `none`
   - Decision: `keep`
2. `#102`
   - Expected: `rats-the-video-game/index.html`, `.Jules/palette-rat.md`
   - Unexpected: `none`
   - Decision: `keep`
3. `#99`
   - Expected: consolidated versions of the squeak and modal updates
   - Unexpected: `.Jules/ink.md`, `.Jules/nibble.md`, extra modal button styling not represented in the refreshed source PRs
   - Decision: `superseded; do not merge in this run`
4. `#14`
   - Expected: experiment code, website wiring, workflow/build updates, docs, journal updates
   - Unexpected: none identified during this audit
   - Decision: `manual-follow-up; exclude from automated execute scope`

## Required Fixes Before Merge

1. `#101`: no code fix identified during audit.
2. `#102`: generate an integration-branch commit message in Conventional Commit format because the source-branch commit headline is not conformant.
3. `#99`: close as superseded once the replacement integration branch or PR is ready.
4. `#14`: keep deferred until a separate manual review decides whether to refresh it onto current `main` or replace it with a smaller follow-up.

## Label Plan

1. `#101` -> `gameplay`
2. `#102` -> `ux`, `accessibility`
3. `#99` -> retain `consolidation` until closure comment, then close as superseded
4. `#14` -> retain `manual-follow-up`

## Label Review Checks

1. Required labels present: `yes`
2. Incorrect or stale labels found: `#101` and `#102` are unlabelled; `#99` is stale relative to the refreshed queue
3. Label updates required before merge: `#101` add `gameplay`; `#102` add `ux` and `accessibility`; `#14` retain `manual-follow-up`
4. Leftover-manual label required (`manual-follow-up`): `yes`

## Label Bootstrap Checks

1. `changes-requested` label exists: `yes`
2. If created during audit, use description exactly: `Review feedback requested before merge`
3. `manual-follow-up` label exists: `yes`
4. If created during audit, use description exactly: `Needs manual attention outside automated integration workflow`

## PR Title Checks

Current stack result: `fail`

1. `#101` and `#102` use themed prefixes and bracketed descriptors instead of the preferred outcome-first style.
2. `#99` already uses the preferred integration-PR style, but its scope is stale compared with the refreshed queue.
3. Execute mode should keep the integration PR title outcome-first even if the source PR titles remain unchanged.

## PR Description Checks

1. `#101` and `#102` both explain the intended player-facing outcome clearly.
2. The automation footer is acceptable because it does not expose internal queue labels or integration triage notes.
3. `#99` accurately documents the older run, but it should not be treated as the canonical batch any longer.

## Integration PR Title Policy

Recommended integration PR title:

1. `Add squeak input and improve modal touch targets (#101, #102)`

## Mergeability Checks

1. `#101`: `CLEAN`
   - GitHub merge state is `CLEAN`.
   - The branch scope is coherent and ready for integration.
2. `#102`: `CLEAN`
   - GitHub merge state is `CLEAN`.
   - The branch scope is coherent, but the source commit headline is not Conventional Commit compliant.
3. `#99`: `BLOCKED`
   - GitHub currently reports the PR as mergeable.
   - Audit blocks it from this run because it duplicates `#101` and `#102` while adding broader stale scope.
4. `#14`: `BLOCKED`
   - GitHub merge state is `DIRTY`.
   - This PR remains outside the automated stack by design.

## Extreme Conflict Case Handling

1. `#101`
   - Rebase onto `main` status: `not run during audit; GitHub reports CLEAN`
   - Integration-branch merge status: `expected pass`
   - Review state: `other`
   - Fate: `merge`
2. `#102`
   - Rebase onto `main` status: `not run during audit; GitHub reports CLEAN`
   - Integration-branch merge status: `expected pass after #101`
   - Review state: `other`
   - Fate: `merge`
3. `#99`
   - Rebase onto `main` status: `not run during audit; GitHub reports CLEAN`
   - Integration-branch merge status after rebase: `expected duplicate/conflicting scope`
   - Review state: `other`
   - Fate: `close`
4. `#14`
   - Rebase onto `main` status: `not run during audit`
   - Integration-branch merge status after rebase: `unknown`
   - Review state: `other`
   - Fate: `defer`

## Scope Coherence and Split Plan

1. `#101`: `coherent`
   - Keep `rats-the-video-game/src/game.js`
   - Keep `rats-the-video-game/index.html`
   - Keep `.Jules/scurry.md`
   - Rationale: the squeak mechanic, help-text update, and Scurry journal change belong together.
2. `#102`: `coherent`
   - Keep `.Jules/palette-rat.md`
   - Keep `rats-the-video-game/index.html`
   - Rationale: the touch-target and overlay-dismissal changes align cleanly with the Palette-Rat journal update.
3. `#99`: `split-not-needed because superseded`
   - No execute-time split plan.
   - Rationale: the branch is broader than the refreshed queue and should be replaced, not reshaped.
4. `#14`: `manual-follow-up`
   - No automated split plan for this batch.
   - Rationale: the experiment is broader than the current gameplay/UI queue and is already labelled for manual handling.

## Commit Message Conformance and Rewrite Plan

1. `#101`: `pass`
   - Commit headline: `feat: add squeak mechanic to scare birds`
   - Rewrite method: `none planned`
   - Force-push implied: `no`
2. `#102`: `fail if merged directly`
   - Commit headline: `🎨🐀 Palette-Rat: [UX improvement] increase touch targets and add modal…`
   - Rewrite method: `avoid source rewrite; use a Conventional Commit message on the integration branch merge/cherry-pick step`
   - Force-push implied: `no`
3. `#99`: `pass`
   - All branch commits are already in acceptable Conventional Commit or docs-scoped form.
   - Rewrite method: `none planned because branch is slated for closure`
   - Force-push implied: `no`
4. `#14`: `fail if merged directly`
   - At least one commit headline is non-conformant (`Implement amplitude-weighted playback and smoothing`)
   - Rewrite method: `not planned in this batch`
   - Force-push implied: `no`

## Force-Push Approval Plan

1. No force-push is recommended for the approved execute path.
2. If the user prefers to fix `#102` in place instead of integrating through a fresh branch, explicit approval will be required before any history rewrite.

## PR Communication Plan

1. `#101`
   - Processing status comment planned: `yes`
   - Final fate target: `merge`
   - Final fate rationale:
     - coherent gameplay addition
     - minimal overlap after ordered integration
   - Closure message required: `no`
   - Backtick-safe message handling required: `no`
2. `#102`
   - Processing status comment planned: `yes`
   - Final fate target: `merge`
   - Final fate rationale:
     - coherent UX improvement
     - minimal overlap after `#101`
     - integration branch can preserve commit-message policy without rewriting the source branch
   - Closure message required: `no`
   - Backtick-safe message handling required: `no`
3. `#99`
   - Processing status comment planned: `yes`
   - Final fate target: `close`
   - Final fate rationale:
     - superseded by narrower refreshed PRs `#101` and `#102`
     - contains broader stale scope not represented in the current queue
   - Closure message required: `yes`
   - Backtick-safe message handling required: `no`
4. `#14`
   - Processing status comment planned: `no`
   - Final fate target: `defer`
   - Final fate rationale:
     - already labelled `manual-follow-up`
     - outside the gameplay/UI batch
   - Closure message required: `no`
   - Backtick-safe message handling required: `no`

## PR Comment Templates

### Processing Comment Template

```text
Automatically processed by the PR integration workflow.

Current status: <audit-approved|executing|blocked>
Planned actions: <rebase/label updates/merge/close>
```

### Final Fate Comment Template

```text
Automatically processed by the PR integration workflow.

Final status: <merged|closed|deferred>
Reason:
- <reason one>
- <reason two>

Next steps:
- <optional follow-up>
```

## Merge Order Proposal

1. `#101` merge first because it introduces the squeak mechanic and shared help-text changes.
2. `#102` merge second because it touches the same HTML surface but should layer cleanly after `#101`.
3. `#99` close after the replacement integration branch or PR exists so the history of the earlier attempt remains understandable.
4. `#14` leave deferred.

## Integration PR Metadata

1. Planned integration PR title: `Add squeak input and improve modal touch targets (#101, #102)`
2. Title policy check: `pass`
3. Reference link format check (`#<PR>`): `pass`

## Planned Commands (Dry-Run)

```bash
# Preflight
gh auth status
git switch main
git pull --ff-only
git status --short --branch

# Branch setup
git switch -c integration/squeak-touch-targets-refresh

# Per-PR integration
gh pr view 101
git merge --no-ff origin/scurry-squeak-mechanic-1844732970679861154
gh pr view 102
git merge --no-ff origin/palette-rat-touch-targets-modals-1212973008546341614

# Validation
pnpm --dir rats-the-video-game test
pnpm --dir the-lab-website build
```

Run critical git operations sequentially for:

- `git switch`
- `git merge`
- `git rebase`
- `git cherry-pick`

## Execution Gates

All must pass before `execute` mode:

1. Preflight checks pass.
2. CI checks pass on latest PR heads.
3. Required review policy passes.
4. Working tree is clean.
5. Plan approved by reviewer.
