# PR Integration Action Plan Artefact

## Purpose

Audit of the current open PR queue for `liminal-hq/the-lab`, focused on the post-`#96` gameplay and modal UX follow-up stack.

```text
 (\_/)
 (o.o)  Rat says: squeak first, then tidy the modal wires.
 (> <)
```

## Run Metadata

- Run date: `2026-03-12`
- Repository: `liminal-hq/the-lab`
- Mode: `audit`
- Requested by: `scott`
- Generated by: `Codex GPT-5`
- Planned integration branch: `integration/bird-squeak-modal-ux`
- Branch naming check: `pass`

## Main Snapshot

- Current branch: `main`
- Current `main` head: `0151a50` (`🐀 Rat Cleanup: Consolidate gameplay, accessibility, verification, and docs (#57, #60, #61, #62, #63, #65, #85, #86, #87, #88, #94, #95) (#96)`)
- Working tree clean: `yes`
- Audit started from `main`: `yes`
- Branch protection: `none configured`
- Allowed merge methods: `merge`, `rebase`

## Queue Summary

Open queue shape:

- Active automated queue: `#97`, `#98`
- Deferred manual queue: `#14` (`manual-follow-up`)

Audit conclusion:

1. The active queue is small enough for one integration batch, but only after planned pruning and split work.
2. `#97` is close to mergeable as-is, including its intentional Scurry journal update.
3. `#98` should not be merged as-is; its `rats-the-video-game/index.html` UX changes and Palette-Rat journal update belong in the batch, but the root helper scripts and screenshots do not.
4. `#14` remains deferred outside this automated run and should keep its `manual-follow-up` label.

## PR Inventory

1. `#97` `🐀 Scurry: [mechanic] Bird Scare Squeak`
   - Branch: `scurry-bird-scare-9059667324379963487` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `none`
   - Changed files: `3`
2. `#98` `🎨🐀 Palette-Rat: [UX improvements: checkboxes, modal overlay]`
   - Branch: `palette-rat-ux-improvements-952982666946918618` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `none`
   - Changed files: `10`
3. `#14` `Add Rats Music Converter`
   - Branch: `rats-music-converter-9930008502543285091` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `documentation`, `enhancement`, `ci`, `manual-follow-up`
   - Changed files: `15`

## Scoring

Scoring was applied to the active automated queue only.

1. `#97`: `10`
2. `#98`: `10`

Scoring notes:

1. Both PRs earn `+30` for player-facing improvements.
2. Both PRs take `-20` for overlapping in `rats-the-video-game/index.html`.

## Conflict Matrix

| File | PRs touching file | Conflict risk |
| --- | --- | --- |
| `rats-the-video-game/index.html` | `#97, #98` | `high` |
| `rats-the-video-game/src/game.js` | `#97` | `low` |
| `.Jules/scurry.md` | `#97` | `low` |
| `.Jules/palette-rat.md`, root helper scripts, root screenshots | `#98` | `low` |

Conflict resolution notes:

1. Integrate the squeak mechanic first so the shared tutorial/help text settles before modal UX refinements land.
2. Treat `.Jules/*.md` journal files as intentional logs when they accompany related behaviour changes; do not treat them as stray review noise.
3. Keep `#14` out of this batch entirely because it is already deferred and `DIRTY` against current `main`.

## Allowed-File Guardrail Check

1. `#97`
   - Expected: `rats-the-video-game/src/game.js`, `rats-the-video-game/index.html`, `.Jules/scurry.md`
   - Unexpected: `none`
   - Decision: `keep`
2. `#98`
   - Expected: `rats-the-video-game/index.html`, `.Jules/palette-rat.md`
   - Unexpected: `fix_modal.py`, `test_edit.py`, `test_ui.py`, `test_ui2.py`, `test_ui3.py`, `ui_screenshot.png`, `ui_screenshot_2.png`, `ui_screenshot_options.png`
   - Decision: `split-needed; retain product HTML plus the related agent log on a fresh branch`
3. `#14`
   - Expected: experiment code, website wiring, workflow/build updates, docs, journal updates
   - Unexpected: none identified during this audit
   - Decision: `manual-follow-up; exclude from automated execute scope`

## Required Fixes Before Merge

1. `#97`: use an outcome-style PR title if a direct representative PR is reopened later.
2. `#98`: port `rats-the-video-game/index.html` plus `.Jules/palette-rat.md`; leave the root helper scripts and generated screenshots out of the repo history for this batch.
3. `#98`: replace the square-bracket title style before direct merge or closure messaging so the queue remains outcome-first and stylistically consistent.
4. `#14`: keep deferred until a separate manual review decides whether to refresh it onto current `main` or replace it with a smaller follow-up.

## Label Plan

1. `#97` -> `consolidation`
2. `#98` -> `changes-requested`
3. `#14` -> retain `manual-follow-up`

## Label Review Checks

1. Required labels present: `yes`
2. Incorrect or stale labels found: `#97` and `#98` are currently unlabelled despite clear automated fates
3. Label updates required before merge: `#97` add `consolidation`; `#98` add `changes-requested`; `#14` retain `manual-follow-up`
4. Leftover-manual label required (`manual-follow-up`): `yes`

## Label Bootstrap Checks

1. `changes-requested` label exists: `yes`
2. If created during audit, use description exactly: `Review feedback requested before merge`
3. `manual-follow-up` label exists: `yes`
4. If created during audit, use description exactly: `Needs manual attention outside automated integration workflow`

## PR Title Checks

Current stack result: `fail`

1. `#97` and `#98` both use square-bracket process tags.
2. `#14` lacks the current themed title style and remains intentionally outside the active stack.
3. The queue should move to outcome-first titles without process brackets before any direct merge path.

## PR Description Checks

1. `#97` and `#98` use outcome-focused language and describe the player-facing change clearly.
2. The Jules task footer is acceptable and does not expose internal integration-planning notes.
3. `#98` mentions screenshot verification, but the actual screenshot artefacts still do not belong in the merge payload.

## Integration PR Title Policy

Recommended integration PR title:

1. `Add bird-scare squeak and improve modal touch UX (#97, #98)`

## Mergeability Checks

1. `#97`: `CLEAN`
   - GitHub merge state is `CLEAN`, and the branch contents are acceptable for integration.
2. `#98`: `DIRTY`
   - GitHub merge state is `CLEAN`, but the branch needs a split because the intended UX changes are bundled with root-level helper scripts and generated screenshots.
3. `#14`: `BLOCKED`
   - GitHub merge state is `DIRTY`, and the PR remains outside the automated stack by design.

## Extreme Conflict Case Handling

1. `#97`
   - Rebase onto `main` status: `not run during audit; GitHub reports CLEAN`
   - Integration-branch merge status: `expected pass`
   - Review state: `other`
   - Fate: `merge`
2. `#98`
   - Rebase onto `main` status: `not run during audit; GitHub reports CLEAN`
   - Integration-branch merge status after scoped port: `expected pass`
   - Review state: `other`
   - Fate: `changes-requested on current PR; port intended behaviour from a clean branch`
3. `#14`
   - Rebase onto `main` status: `not run during audit`
   - Integration-branch merge status after rebase: `unknown`
   - Review state: `other`
   - Fate: `defer`

## Scope Coherence and Split Plan

1. `#97`: `coherent`
   - Keep `rats-the-video-game/src/game.js`
   - Keep `rats-the-video-game/index.html`
   - Keep `.Jules/scurry.md`
   - Rationale: the player-facing mechanic, help-text changes, and Scurry journal update belong together.
2. `#98`: `split-needed`
   - Keep `rats-the-video-game/index.html`
   - Keep `.Jules/palette-rat.md`
   - Exclude `fix_modal.py`
   - Exclude `test_edit.py`
   - Exclude `test_ui.py`
   - Exclude `test_ui2.py`
   - Exclude `test_ui3.py`
   - Exclude `ui_screenshot.png`
   - Exclude `ui_screenshot_2.png`
   - Exclude `ui_screenshot_options.png`
   - Rationale: the UX improvement and Palette-Rat journal update are valid, but the rest is local scaffolding and generated output rather than reviewable product code.
3. `#14`: `manual-follow-up`
   - No automated split plan for this batch.
   - Rationale: the experiment is broader than the current gameplay/UI queue and is already labelled for manual handling.

## Commit Message Conformance and Rewrite Plan

1. `#97`: `pass`
   - Commit headline: `feat: implement squeak mechanic to scare birds`
   - Rewrite method: `none planned`
   - Force-push implied: `no`
2. `#98`: `pass`
   - Commit headline: `feat(ui): improve touch targets and modal accessibility`
   - Rewrite method: `none planned`
   - Force-push implied: `no`
3. `#14`: `fail if merged directly`
   - Mixed commit quality and at least one non-Conventional Commit headline (`Implement amplitude-weighted playback and smoothing`)
   - Rewrite method: `not planned in this batch`
   - Force-push implied: `no`

## Force-Push Approval Plan

1. No force-push is recommended for the approved execute path.
2. If `#98` or `#14` is later rewritten in place instead of ported to a fresh branch, approval will be required before any force-push.

## PR Communication Plan

1. `#97`
   - Processing status comment planned: `yes`
   - Final fate target: `merge`
   - Final fate rationale:
     - coherent gameplay addition
     - related Scurry journal update is intentional
   - Closure message required: `no`
   - Backtick-safe message handling required: `no`
2. `#98`
   - Processing status comment planned: `yes`
   - Final fate target: `defer current PR`
   - Final fate rationale:
     - intended UX change is valid
     - root helper scripts and generated screenshots should not ship
     - a clean scoped follow-up is safer than merging the branch as-is
   - Closure message required: `no`
   - Backtick-safe message handling required: `no`
3. `#14`
   - Processing status comment planned: `no`
   - Final fate target: `defer`
   - Final fate rationale:
     - already labelled `manual-follow-up`
     - outside the gameplay/UI batch
   - Closure message required: `no`
   - Backtick-safe message handling required: `no`

## PR Comment Templates

### Processing Comment Template

```text
Automatically processed by the PR integration workflow.

Current status: audit-approved
Planned actions: <prune/split/label updates/merge>
```

### Final Fate Comment Template

```text
Automatically processed by the PR integration workflow.

Final status: <merged|closed|deferred>
Reason:
- <reason one>
- <reason two>

Next steps:
- <optional follow-up>
```

## Merge Order Proposal

1. `#97` first because it is the smaller, cleaner branch and establishes the shared squeak instructions in `index.html`.
2. `#98` second as a scoped port after the noisy helper and screenshot files are excluded.

## Integration PR Metadata

1. Planned integration PR title: `Add bird-scare squeak and improve modal touch UX (#97, #98)`
2. Title policy check: `pass`
3. Reference link format check (`#<PR>`): `pass`

## Planned Commands (Dry-Run)

```bash
# Preflight
gh auth status
git switch main
git pull --ff-only
git status --short --branch

# Branch setup
git switch -c integration/bird-squeak-modal-ux

# Representative PR inspection
gh pr view 97 --json files,commits,statusCheckRollup
gh pr view 98 --json files,commits,statusCheckRollup

# Planned execute path after approval
gh pr checkout 97
git switch integration/bird-squeak-modal-ux
git cherry-pick -n cb59617b32870a0627329a52e823a52f89d748bc

gh pr checkout 98
git switch integration/bird-squeak-modal-ux
git checkout palette-rat-ux-improvements-952982666946918618 -- rats-the-video-game/index.html .Jules/palette-rat.md

# Validation
/home/scott/.nvm/versions/node/v22.21.1/bin/pnpm --dir the-lab-website build
/home/scott/.nvm/versions/node/v22.21.1/bin/pnpm --dir rats-the-video-game test
```

## Execution Gates

All must pass before `execute` mode:

1. Preflight checks pass.
2. CI checks pass on the latest retained PR heads.
3. Label updates are applied as planned.
4. Working tree is clean before mutating commands begin.
5. The split plan for `#98` is explicitly approved.
