# PR Integration Action Plan Artefact

## Purpose

Audit of the full open PR queue for `liminal-hq/the-lab`: **132 open PRs** (`#14`, `#104`-`#234`), almost all authored by `google-labs-jules[bot]` across five recurring "stuck loop" agent personas (Archivist, Ink, Tunneller, Chord, Nibble) plus two "genuinely iterating" feature personas (Scurry, Palette-Rat). None of these PRs were ever merged, so the recurring personas kept re-proposing the same change weekly, each running statelessly with no visibility into its own prior attempts or the other 130+ open PRs.

An initial audit pass only queried the 50 most recent PRs (`--limit 50` silently truncated `gh pr list`) and undercounted the backlog by more than half. This is the corrected, full-queue audit.

```text
  __
 (oo)  Rat says: one hundred and thirty-two squeaks, but only eighteen songs.
 /\/\
```

## Run Metadata

- Run date: `2026-07-08`
- Repository: `liminal-hq/the-lab`
- Mode: `audit`
- Requested by: `scott`
- Generated by: `Claude Sonnet 5`
- Planned integration branch: `integration/rats-backlog-consolidation`
- Branch naming check: `pass`

## Main Snapshot

- Current branch: `main`
- Working tree clean: `yes`
- Audit started from `main`: `yes`
- Branch protection: `none configured`
- Allowed merge methods: `merge`, `rebase`

## Queue Shape

132 open PRs collapse into **17 distinct changes to integrate**, **1 deferred** (`#14`, already labelled `manual-follow-up` from a prior run), and **114 duplicates/superseded PRs to close**.

| Theme | Persona | Open PRs | Distinct outcomes | Close |
| --- | --- | --- | --- | --- |
| Docs | Archivist | 17 | 2 (`#232` hazard docs, `#168` double-jump/mobile-squeak docs) | 15 |
| Visual journal | Ink | 17 | 2 (`#233` journal entry 13, `#169` new SFX asset in `rats_action.svg`) | 15 |
| Deterministic seeding | Tunneller | 17 | 1 (`#231`) | 16 |
| Audio SFX | Chord | 17 | 3 (`#192` metal chew, `#230` splat, `#171` construction-district music fix) | 14 |
| Test verification | Nibble | 15 | 2 (`#196` touch-input, `#201` squeak) | 13 |
| Gameplay mechanics | Scurry | 24 | 5 (`#117` squeak-shatter, `#113` coffee/hazard, `#154` bottle-cap, `#207` stun, `#228` speed-trail) | 19 |
| Canvas/modal a11y | Palette-Rat | 24 | 2 (`#160` modal-focus-stack, `#229` canvas-focus) | 22 |
| Deferred (pre-existing) | — | 1 (`#14`) | 0 | 0 (defer) |
| **Total** | | **132** | **17** | **114** |

All 132 PRs report `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN` against current `main` — no rebases required for any PR in this batch, including the March-dated ones.

## PR Inventory and Scoring

### Docs (Archivist) — keep `#232` + `#168`

All 17 diff from the same base `docs/mechanics.md` blob. Two distinct gaps exist in current docs: the Birds/Turds hazard + mobile Squeak control (won by `#232`, the fullest single-file version), and the undocumented Double Jump mechanic + missing mobile Squeak control line (won by `#168`, which cleanly explains in its commit message that "these controls were previously present in index.html and implemented in game.js, but omitted from the mechanics.md spec document"). `#168` and `#232` both add the same one-line mobile Squeak control bullet — dedupe to one copy during integration.

- Score: `#232` `+10` (docs) `+30` (closes a real gap); `#168` `+10` (docs) `+20` (closes a second real gap, ships with its own `.Jules/archivist.md` provenance note).
- Close as superseded: `110, 129, 148, 163, 173, 178, 185, 189, 194, 199, 204, 211, 216, 221, 226` (15).
  - `#226` additionally writes `.Jules/archivist.md` to the repo root instead of `rats-the-video-game/.Jules/` — a scope violation on top of being a duplicate.
  - `#216` uniquely also touched `README.md`; verify after integration whether the README still needs a "swipe down to squeak" Quick Start line (`#168`/`#232` only touch `docs/mechanics.md`, not `README.md`) and add it as a trivial one-line fix if so.

### Visual journal (Ink) — keep `#233` + `#169`

16 of the 17 diff the identical `assets/images/lab_journal.svg` blob, filling the same journal-entry-13 placeholder with different wording for the same underlying feature (squeak/modal touch polish) — not cumulative, mutually exclusive edits to the same two lines. `#233` wins that cluster (only one that also adds a durable process note to `.Jules/ink.md`, no stray scratch files).

`#169` is genuinely different: it adds a new "SQUEAK!" effect and scared-bird-flying-away graphic to `assets/images/rats_action.svg` — an asset untouched by any other Ink PR, confirmed absent from current `main`.

- Score: `#233` `+30` (keeps living journal moving); `#169` `+30` (net-new illustration, no overlap).
- Close as superseded: `111, 130, 149, 164, 174, 179, 186, 190, 195, 200, 205, 212, 217, 222, 227` (15).
  - `#205` leaves a stray `commit_msg.txt` scratch file — one more reason among the 15 to prefer the keepers.

### Deterministic seeding (Tunneller) — keep `#231`

All 17 implement the same `cyrb128` hash + `mulberry32` PRNG seeded via a `?seed=` URL param. `#231` is the only one shipping a Playwright regression test (`tests/verify_seed.spec.js`), with a clean module-scoped `levelPRNG` (no `Math`/`window` pollution, unlike several losers).

- Score: `+40` (deterministic generation) `+10` (test coverage).
- Close as superseded: `107, 126, 145, 162, 167, 172, 177, 182, 188, 193, 198, 203, 210, 215, 220, 225` (16).

### Audio SFX (Chord) — keep `#192`, `#230`, `#171`

`#192` (metallic chew for `PRIUS` obstacle) and `#230` (splat sound for turd collisions, winner of an 8-way `playSplat`/`playOuch` duplicate cluster) are unchanged from the prior pass.

**New finding:** `#171` ("Sync district motifs and add Construction audio/variation") is not a duplicate of either — current `main`'s `audio.js` has SURFACE-district music thresholds (`cycle>=17/9`) that are stale relative to `game.js`'s actual district boundaries (`19/13/7`), and the Construction district has **no music at all**. `#171` fixes this real, still-present bug with a minimal 9-line diff (its duplicate, `#161`, does the same fix but drags in unrelated `package-lock.json` churn).

- Score: `#192` `+30`; `#230` `+30`; `#171` `+60` (bug fix — missing audio branch, not just polish).
- Close as superseded: `106, 125, 144, 161, 166, 176, 181, 187, 197, 202, 209, 214, 219, 224` (14).

### Test verification (Nibble) — keep `#196` + `#201`

`#196` (`verify_touch_input.spec.js`, mobile `TouchEvent` handling) has no overlap with the squeak-verification cluster. `#201` wins a 12-way duplicate cluster testing the same squeak-scares-birds mechanic — most robust sync (`waitForFunction` vs. fixed `setTimeout`) and the only one covering three scenarios including an already-fleeing bird's `vy` preservation.

- Required fix before merge: `#201` writes `.Jules/nibble.md` under `rats-the-video-game/.Jules/`; convention (commit `2a71d63`) is repo-root `.Jules/nibble.md` — copy the corrected path from `#213` during integration.
- Score: `#196` `+10`; `#201` `+10`.
- Close as superseded: `112, 150, 165, 170, 175, 180, 191, 206, 213, 218, 223, 234` (12), plus stray-named `#131` (13 total... — see note).
  - Note: `#131`'s branch (`jules-5785695617811326174-a01f19dd`) doesn't follow the `nibble-*` naming convention but its content is a standard squeak-AoE duplicate test. Included in the close count above.

### Gameplay mechanics (Scurry) — keep `#117`, `#113`, `#154`, `#207`, `#228`

Four sub-clusters, not one:

1. **Squeak shatters projectiles** (14 PRs) — the mechanic does not exist on `main` today. `#117` wins: the only implementation with a Playwright test that follows project conventions (relative URL + ESM import, unlike `#134`'s hardcoded URL + CommonJS), correct Euclidean-distance shatter radius, and complete doc/journal/index.html updates in one coherent diff.
2. **Coffee/hazard interaction** (2 PRs, `#113` and `#115`) — both implement "lose Coffee speed boost on hitting a trap/third-rail," previously missed as two unrelated singletons. `#113` wins: adds spilled-coffee particle feedback that `#115` lacks, otherwise identical.
3. **Bottle Cap micro-collectible** (6 PRs) — not on `main` today. `#154` wins: the only one with a real breadcrumb-trail spawn design (arcs over traps/springs to guide jump timing) rather than a single random spawn, plus a dedicated `playClink()` SFX and correct doc/journal updates.
4. **Singleton mechanics** (2 PRs, non-overlapping with each other or the above) — `#207` (iFrames + knockback stun after hazard hits) and `#228` (speed-boost particle trail).

- Score: `#117` `+60` (real mechanic gap); `#113` `+30` (risk/reward polish); `#154` `+30` (new collectible); `#207` `+60` (fairness/bug-adjacent fix); `#228` `+30` (UX polish).
- Close as superseded: `104, 108, 115, 119, 121, 123, 127, 132, 134, 136, 138, 140, 142, 146, 151, 155, 157, 159, 183` (19).

**Integration risk:** `#207` and `#113` both modify the exact same `TRAP`/`THIRD_RAIL` collision-handling block in `game.js` (stun-gating vs. coffee-boost-loss). This will need a manual (not auto) merge combining both effects — flagged as a required fix below, not a blocker.

### Canvas/modal a11y (Palette-Rat) — keep `#160` + `#229`

Not 24 copies of one fix — two real sub-features plus one incomplete stub:

- **Nested modal focus stack** (17 PRs, usually bundled with 24px touch targets): current `main` still uses a single `lastFocus` variable that gets overwritten when modals nest (Options → Credits loses the return path). `#160` wins: the only PR in this cluster that ships a Playwright regression test (`tests/modal-focus.spec.js`) alongside the `focusStack` array fix.
- **Debug-toggle checkbox a11y** (4 PRs: missing `for` attribute + sub-24px touch target): `#160` already includes this exact fix as part of its diff, fully subsuming this sub-cluster — no separate PR needed.
- **Incomplete stub:** `#139` contains no real fix (a one-line placeholder comment in a stray `test_script.js`) — close as invalid, not just superseded.
- `#229` (canvas `tabindex`/`role` + refocus-on-modal-close) is complementary, not overlapping — different lines, already confirmed clean.

- Score: `#160` `+30` (accessibility) `+10` (test coverage); `#229` `+30` (accessibility).
- Close as superseded: `105, 109, 114, 116, 118, 120, 122, 124, 128, 133, 135, 137, 139, 141, 143, 147, 152, 153, 156, 158, 184, 208` (22).

### Deferred — `#14`

Already labelled `manual-follow-up` from a prior integration run (Rats Music Converter). Left untouched by this batch.

## Conflict Matrix

| File | PRs touching file | Conflict risk |
| --- | --- | --- |
| `rats-the-video-game/src/game.js` | `#231, #117, #113, #207, #228, #192, #230` | `medium-high` — 7 PRs; `#207`/`#113` hit the identical collision block and need manual combination |
| `rats-the-video-game/src/audio.js` | `#192, #230, #171` | `low` — additive methods + threshold fix, different regions |
| `rats-the-video-game/index.html` | `#160, #229` | `low` — different lines (modal focus stack vs. canvas attrs) |
| `rats-the-video-game/docs/mechanics.md` | `#232, #168, #154(via keep? no)` | `low` — `#232`/`#168` both add the same one mobile-squeak bullet, dedupe |
| `rats-the-video-game/.Jules/scurry.md` | `#117, #113, #154, #207, #228` | `low` — five independent line appends, trivial combine |
| `rats-the-video-game/.Jules/chord.md` | `#192, #230, #171` | `low` — three independent line appends, trivial combine |
| `assets/images/lab_journal.svg` | `#233` | `none` |
| `assets/images/rats_action.svg` | `#169` | `none` |
| `rats-the-video-game/tests/*.spec.js` | `#231, #196, #201, #117, #160` | `none` — distinct new files |

Conflict resolution notes:

1. Land `#231` (Tunneller) first — most invasive `game.js` change, apply to clean baseline.
2. Land `#117` (Scurry shatter) next — adjacent to but distinct from Tunneller's changes.
3. Land `#207` then `#113` together, resolving the shared collision-block conflict by hand (stun-gate wraps the block; coffee-loss logic nests inside it).
4. Land `#228` (Scurry speed-trail) and `#154` (Scurry bottle-cap) — both small, low-risk additions.
5. Land `#192`, `#230`, `#171` (Chord) — additive audio hooks + threshold fix.
6. Land `#160`, `#229` (Palette-Rat) — independent `index.html` regions.
7. Land `#196`, `#201` (Nibble tests) — independent new files, fix `#201`'s journal path.
8. Land `#232`, then `#168` (Archivist docs) — dedupe the shared mobile-squeak bullet; verify/patch README swipe-down line.
9. Land `#169`, then `#233` (Ink) — visual assets last, journal entry documents the whole batch.

## Allowed-File Guardrail Check

All 17 keepers checked against their PR-stated scope; no unexpected files found except the two already-flagged violations in the *closed* set (`#226` writes `.Jules/archivist.md` to repo root; `#183` similarly misplaces `.Jules/scurry.md`) — both are closed as duplicates regardless, so no guardrail exception needs approval.

## Required Fixes Before Merge

1. `#232` + `#168`: dedupe the duplicate mobile Squeak control bullet in `docs/mechanics.md`.
2. `#168`/`#232`: verify whether `README.md` still needs the "swipe down to squeak" Quick Start line (only `#216`, now closed, had it) — add as a one-line fix if so.
3. `#201`: correct `.Jules/nibble.md` path to repo root, not `rats-the-video-game/.Jules/`.
4. `#207` + `#113`: manually combine the shared `TRAP`/`THIRD_RAIL` collision-block edit (stun-gate + coffee-boost-loss).
5. `#117` + `#113` + `#154` + `#207` + `#228`: manually combine five independent appends to `rats-the-video-game/.Jules/scurry.md`.
6. `#192` + `#230` + `#171`: manually combine three independent appends to `rats-the-video-game/.Jules/chord.md`.

## Label Plan

- Gameplay PRs (`#231, #117, #113, #154, #207, #228, #192, #230, #171`) -> `gameplay`
- A11y PRs (`#160, #229`) -> `ux`, `accessibility`
- Test PRs (`#196, #201`) -> `testing`
- Doc PRs (`#232, #168`) -> `documentation`
- Journal PRs (`#233, #169`) -> `documentation`, `ux`
- Integration PR -> `consolidation`, `gameplay`, `ux`, `accessibility`, `testing`, `documentation`

## Label Bootstrap Checks

1. `changes-requested` label exists: `yes`.
2. `manual-follow-up` label exists: `yes` (already applied to `#14`).

## Integration PR Title Policy

`🐀 Rat Cleanup: Add seeded generation, projectile/hazard/collectible mechanics, distinct SFX, modal/canvas a11y, docs, and journal updates (#113, #117, #154, #160, #168, #169, #171, #192, #196, #201, #207, #228, #229, #230, #231, #232, #233)`

## Mergeability Checks

All 132 PRs report `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN` against current `main`. No rebases required.

## Scope Coherence and Split Plan

All 17 keeper PRs are coherent as-is; no splits required. Five require small in-flight amendments (documented in Required Fixes above): `#232`/`#168` (dedupe + README check), `#201` (journal path), `#207`/`#113` (manual conflict merge).

## Commit Message Conformance and Rewrite Plan

All 17 keeper PRs use themed emoji-prefixed commit subjects, not Conventional Commits. Per `AGENTS.md` rule 2, integration-branch commits will be rewritten to Conventional Commit format with detailed markdown bodies during integration. No force-push of source branches — they are left untouched and closed after the integration PR opens.

## Force-Push Approval Plan

None required — no source PR branch needs rebasing or in-place rewriting.

## PR Communication Plan

- All 17 keeper PRs: processing status comment, final fate `merge` (superseded by the integration PR once merged).
- All 114 duplicate/superseded PRs: no processing comment, final fate `close`, rationale referencing the winning `#<PR>`. Backtick-safe file-based comment input required throughout (`AGENTS.md` rule 2).
- `#14`: left untouched, already `manual-follow-up`.

## Merge Order Proposal

1. `#231` Tunneller — foundational `game.js` PRNG change.
2. `#117` Scurry squeak-shatter.
3. `#207` + `#113` Scurry stun + coffee-hazard-loss — manual combine.
4. `#228` Scurry speed-trail.
5. `#154` Scurry bottle-cap.
6. `#192` Chord metal-chew.
7. `#230` Chord splat.
8. `#171` Chord construction-district music fix.
9. `#160` Palette-Rat modal-focus-stack.
10. `#229` Palette-Rat canvas-focus.
11. `#196` Nibble touch-input test.
12. `#201` Nibble squeak-verify test, path-corrected.
13. `#232` Archivist hazard docs.
14. `#168` Archivist double-jump/mobile docs, deduped against `#232`.
15. `#169` Ink new SFX asset.
16. `#233` Ink journal entry, landed last.

## Integration PR Metadata

1. Planned integration PR title: see above.
2. Title policy check: `pass`.
3. Reference link format check (`#<PR>`): `pass`.

## Planned Commands (Dry-Run)

```bash
# Preflight
gh auth status
git switch main
git pull --ff-only
git status --short --branch

# Branch setup
git switch -c integration/rats-backlog-consolidation

# Per-PR integration (sequential, no parallel git ops) — see Merge Order Proposal for full sequence and branch refs

# Validation (after each merge, and at the end)
pnpm --dir rats-the-video-game test    # via Docker Playwright per established convention
pnpm --dir the-lab-website build
```

Run critical git operations sequentially (no parallel execution) for `git switch`, `git merge`, `git rebase`, `git cherry-pick`.

## Execution Gates

All must pass before `execute` mode:

1. Preflight checks pass.
2. CI checks pass on latest PR heads (confirmed: all `deploy-alpha: SUCCESS`).
3. Working tree is clean.
4. **This plan is approved by Scott.**
5. Push approval explicitly granted by Scott (10 merges of new gameplay/audio code, 1 integration PR, and 114 PR closes are visible, hard-to-reverse actions on the shared GitHub repo).
6. Integration branch name matches `integration/<activity-description>`.
7. Integration PR title policy check passes.
8. Required fixes (dedupe, README check, journal path, manual collision-block combine, `.Jules/*.md` combines) applied before the integration PR opens.

## Rollback Plan

If any validation gate fails during execution: stop, revert the most recent integration-branch commit, re-run `pnpm test`/`pnpm build`, regenerate this artefact with updated state.

## Approval

- Reviewer: `scott`
- Decision: `approved`
- Timestamp: `2026-07-08T05:00:00Z`
- Notes: Executed as planned. A high-effort code review before merge surfaced 8 real bugs from combining independently-authored PRs (seeded-PRNG gap, district-music off-by-one, stun/friction compounding, spring bypass, unconditional turd-splice-while-stunned, doubled touch-squeak audio, a docs/audio mismatch, and one more misplaced journal file) - all fixed in a follow-up commit and re-validated before merge. See `2026-07-08-rats-backlog-consolidation-execution-report.md` for the full outcome.
