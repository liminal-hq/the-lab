# PR Integration Action Plan Artefact

## Purpose

Re-audit of the current open PR queue for `liminal-hq/the-lab` after PR `#99` was merged into `main` on `2026-03-12 22:18 EDT` (`2026-03-13T02:18:15Z`).

```text
  __
 (oo)  Rat says: one merge landed, but a couple of squeaks are still loose.
 /\/\
```

## Run Metadata

- Run date: `2026-03-12`
- Repository: `liminal-hq/the-lab`
- Mode: `audit`
- Requested by: `scott`
- Generated by: `Codex GPT-5`
- Planned integration branch: `integration/squeak-touch-targets-follow-up`
- Branch naming check: `pass`

## Main Snapshot

- Current branch: `main`
- Current `main` head: `3679da5` (`Add bird-scare squeak and improve modal touch UX (#97, #98) (#99)`)
- Working tree clean: `no; audit artefacts only`
- Audit started from `main`: `yes`
- Branch protection: `none configured`
- Allowed merge methods: `merge`, `rebase`

## Queue Summary

Open queue shape:

- Active automated queue: `#101`, `#102`
- Deferred manual queue: `#14`
- Recently merged reference PR: `#99`

Audit conclusion:

1. `#99` is no longer in the open queue, but it did not fully subsume `#101` and `#102`.
2. `#101` still contains follow-up gameplay and copy changes not present on `main`, including mobile swipe-down squeak behaviour and related help/journal updates.
3. `#102` still contains follow-up accessibility and dismissal behaviour not present on `main`, including 24x24 checkbox targets, overlay-click dismissal, and the matching Palette-Rat journal note.
4. Both `#101` and `#102` are now `DIRTY` against `main`, so execute mode must plan rebases and explicit force-push approval before integration.
5. `#14` remains outside this run and should keep `manual-follow-up`.

## PR Inventory

1. `#101` `🧱 Scurry: [squeak mechanic]`
   - Branch: `scurry-squeak-mechanic-1844732970679861154` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `none`
   - Changed files vs current `main`: `3`
2. `#102` `🎨🐀 Palette-Rat: [UX improvement] Increase touch targets and add modal overlay dismissal`
   - Branch: `palette-rat-touch-targets-modals-1212973008546341614` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `none`
   - Changed files vs current `main`: `2`
3. `#14` `Add Rats Music Converter`
   - Branch: `rats-music-converter-9930008502543285091` -> `main`
   - CI status: `deploy-alpha: SUCCESS`, `cleanup-alpha: SKIPPED`
   - Review status: `other`
   - Labels: `documentation`, `enhancement`, `ci`, `manual-follow-up`
   - Changed files: `15`

## Scoring

Scoring notes:

1. This model gives explicit positive weight to UX work but not to net-new gameplay mechanics, so `#101` stays neutral unless overlap risk applies.
2. `#102` takes a conflict penalty because it overlaps the same HTML file touched by `#101`.

1. `#102`: `10`
2. `#101`: `0`
3. `#14`: `-70`

## Conflict Matrix

| File | PRs touching file | Conflict risk |
| --- | --- | --- |
| `rats-the-video-game/index.html` | `#101, #102` | `high` |
| `.Jules/scurry.md` | `#101` | `low` |
| `rats-the-video-game/src/game.js` | `#101` | `low` |
| `.Jules/palette-rat.md` | `#102` | `low` |

Conflict resolution notes:

1. Rebase `#101` onto `origin/main` first and land it before `#102`, because the shared help text sits in `rats-the-video-game/index.html`.
2. Rebase `#102` after `#101` lands or port it to the integration branch with a fresh Conventional Commit message if direct merge remains noisy.
3. Keep `#14` out of this batch entirely because it is already labelled `manual-follow-up`.

## Allowed-File Guardrail Check

1. `#101`
   - Expected: `rats-the-video-game/src/game.js`, `rats-the-video-game/index.html`, `.Jules/scurry.md`
   - Unexpected: `none`
   - Decision: `keep`
2. `#102`
   - Expected: `rats-the-video-game/index.html`, `.Jules/palette-rat.md`
   - Unexpected: `none`
   - Decision: `keep`
3. `#14`
   - Expected: experiment code, website wiring, workflow/build updates, docs, journal updates
   - Unexpected: none identified during this audit
   - Decision: `manual-follow-up; exclude from automated execute scope`

## Required Fixes Before Merge

1. `#101`: rebase onto `origin/main` because GitHub now reports the branch as `DIRTY`.
2. `#102`: rebase onto `origin/main` because GitHub now reports the branch as `DIRTY`.
3. `#102`: replace the non-Conventional source commit with an integration-branch Conventional Commit or rewrite the source branch in place.
4. `#14`: keep deferred until a separate manual review decides whether to refresh it onto current `main` or replace it with a smaller follow-up.

## Label Plan

1. `#101` -> `gameplay`
2. `#102` -> `ux`, `accessibility`
3. `#14` -> retain `manual-follow-up`

## Label Review Checks

1. Required labels present: `yes`
2. Incorrect or stale labels found: `#101` and `#102` are unlabelled despite clear topical ownership
3. Label updates required before merge: `#101` add `gameplay`; `#102` add `ux` and `accessibility`; `#14` retain `manual-follow-up`
4. Leftover-manual label required (`manual-follow-up`): `yes`

## Label Bootstrap Checks

1. `changes-requested` label exists: `yes`
2. If created during audit, use description exactly: `Review feedback requested before merge`
3. `manual-follow-up` label exists: `yes`
4. If created during audit, use description exactly: `Needs manual attention outside automated integration workflow`

## PR Title Checks

Current stack result: `fail`

1. `#101` and `#102` still use themed prefixes and bracketed descriptors instead of the preferred outcome-first style.
2. Execute mode should use an outcome-first integration PR title even if the source PR titles remain unchanged.

## PR Description Checks

1. `#101` and `#102` both explain the intended player-facing outcome clearly.
2. The automation footer is acceptable because it does not expose internal queue labels or integration triage notes.
3. `#14` remains broad and should stay outside this batch.

## Integration PR Title Policy

Recommended integration PR title:

1. `Improve squeak controls and modal touch dismissal (#101, #102)`

## Mergeability Checks

1. `#101`: `DIRTY`
   - GitHub merge state is `DIRTY` after `#99` merged into `main`.
   - The branch still contains meaningful follow-up changes and should not be closed as a duplicate.
2. `#102`: `DIRTY`
   - GitHub merge state is `DIRTY` after `#99` merged into `main`.
   - The branch still contains meaningful follow-up changes and should not be closed as a duplicate.
3. `#14`: `BLOCKED`
   - GitHub merge state is `UNKNOWN` in the current listing.
   - This PR remains outside the automated stack by design.

## Extreme Conflict Case Handling

1. `#101`
   - Rebase onto `main` status: `required`
   - Integration-branch merge status after rebase: `expected pass`
   - Review state: `other`
   - Fate: `merge`
2. `#102`
   - Rebase onto `main` status: `required`
   - Integration-branch merge status after rebase: `expected pass after #101`
   - Review state: `other`
   - Fate: `merge`
3. `#14`
   - Rebase onto `main` status: `not planned in this batch`
   - Integration-branch merge status after rebase: `unknown`
   - Review state: `other`
   - Fate: `defer`

## Scope Coherence and Split Plan

1. `#101`: `coherent`
   - Keep `rats-the-video-game/src/game.js`
   - Keep `rats-the-video-game/index.html`
   - Keep `.Jules/scurry.md`
   - Rationale: the follow-up squeak input behaviour, help copy, and Scurry journal note belong together.
2. `#102`: `coherent`
   - Keep `rats-the-video-game/index.html`
   - Keep `.Jules/palette-rat.md`
   - Rationale: the follow-up touch-target sizing, overlay dismissal, and Palette-Rat journal note belong together.
3. `#14`: `manual-follow-up`
   - No automated split plan for this batch.
   - Rationale: the experiment is broader than the current gameplay/UI queue and is already labelled for manual handling.

## Commit Message Conformance and Rewrite Plan

1. `#101`: `pass`
   - Commit headline: `feat: add squeak mechanic to scare birds`
   - Rewrite method: `none planned`
   - Force-push implied: `yes, because rebase is required`
2. `#102`: `fail if merged directly`
   - Commit headline: `🎨🐀 Palette-Rat: [UX improvement] increase touch targets and add modal dismissal`
   - Rewrite method: prefer an integration-branch commit rewrite or a source-branch `rebase -i --reword`
   - Force-push implied: `yes`
3. `#14`: `fail if merged directly`
   - At least one commit headline is non-conformant (`Implement amplitude-weighted playback and smoothing`)
   - Rewrite method: `not planned in this batch`
   - Force-push implied: `no`

## Force-Push Approval Plan

List every planned force-push operation before execution:

1. `#101` rebase onto `origin/main`
   - Branch: `scurry-squeak-mechanic-1844732970679861154`
   - Command: `git push --force-with-lease origin scurry-squeak-mechanic-1844732970679861154`
   - Approval required: `yes`
2. `#102` rebase onto `origin/main`
   - Branch: `palette-rat-touch-targets-modals-1212973008546341614`
   - Command: `git push --force-with-lease origin palette-rat-touch-targets-modals-1212973008546341614`
   - Approval required: `yes`
3. `#102` commit rewrite in place, if chosen instead of integration-branch rewording
   - Branch: `palette-rat-touch-targets-modals-1212973008546341614`
   - Command: `git push --force-with-lease origin palette-rat-touch-targets-modals-1212973008546341614`
   - Approval required: `yes`

## PR Communication Plan

1. `#101`
   - Processing status comment planned: `yes`
   - Final fate target: `merge`
   - Final fate rationale:
     - residual follow-up gameplay improvements remain after `#99`
     - coherent scope
   - Closure message required: `no`
   - Backtick-safe message handling required: `no`
2. `#102`
   - Processing status comment planned: `yes`
   - Final fate target: `merge`
   - Final fate rationale:
     - residual accessibility and modal behaviour improvements remain after `#99`
     - coherent scope
     - commit-message conformance can be restored during integration
   - Closure message required: `no`
   - Backtick-safe message handling required: `yes`
3. `#14`
   - Processing status comment planned: `no`
   - Final fate target: `defer`
   - Final fate rationale:
     - already labelled `manual-follow-up`
     - outside the gameplay/UI batch
   - Closure message required: `no`
   - Backtick-safe message handling required: `no`

## PR Comment Templates

### Processing Comment Template

```text
Automatically processed by the PR integration workflow.

Current status: <audit-approved|executing|blocked>
Planned actions: <rebase/rewrite/label updates/merge>
```

### Final Fate Comment Template

```text
Automatically processed by the PR integration workflow.

Final status: <merged|closed|deferred>
Reason:
- <reason one>
- <reason two>

Next steps:
- <optional follow-up>
```

## Merge Order Proposal

1. `#101` rebase and integrate first because it introduces the remaining squeak-input follow-up and shared help-text edits.
2. `#102` rebase and integrate second because it touches the same HTML surface and can then resolve against the settled copy.
3. `#14` leave deferred.

## Integration PR Metadata

1. Planned integration PR title: `Improve squeak controls and modal touch dismissal (#101, #102)`
2. Title policy check: `pass`
3. Reference link format check (`#<PR>`): `pass`

## Planned Commands (Dry-Run)

```bash
# Preflight
gh auth status
git switch main
git pull --ff-only
git status --short --branch

# Branch setup
git switch -c integration/squeak-touch-targets-follow-up

# Rebase source PRs
git switch scurry-squeak-mechanic-1844732970679861154
git rebase origin/main
git switch palette-rat-touch-targets-modals-1212973008546341614
git rebase origin/main

# Per-PR integration
git switch integration/squeak-touch-targets-follow-up
git merge --no-ff origin/scurry-squeak-mechanic-1844732970679861154
git cherry-pick origin/palette-rat-touch-targets-modals-1212973008546341614 --no-commit
git commit -m 'feat(rats-the-video-game): improve modal touch targets and overlay dismissal'

# Validation
pnpm --dir rats-the-video-game test
pnpm --dir the-lab-website build
```

Run critical git operations sequentially for:

- `git switch`
- `git merge`
- `git rebase`
- `git cherry-pick`

## Execution Gates

All must pass before `execute` mode:

1. Preflight checks pass.
2. CI checks pass on latest PR heads.
3. Required review policy passes.
4. Working tree is clean aside from approved run artefacts.
5. Plan approved by reviewer.
