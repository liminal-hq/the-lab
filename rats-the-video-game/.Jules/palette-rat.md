- 2026-06-02: In `rats-the-video-game`, to prevent keyboard navigation from interfering with game controls (e.g., Spacebar to jump), explicitly return focus to the `<canvas>` element (which must have `tabindex="0"` and `role="application"`) when closing modals, instead of returning focus to the triggering UI element. This ensures seamless gameplay resumption without re-triggering the previously focused menu button.
